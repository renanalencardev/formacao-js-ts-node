/* 
    Array é um tipo de dado que representa uma lista de valores.
    Pode ser representado por um array literal ou por um array criado com o construtor Array.
*/

let array = [1, 2, 3, 4, 5];
console.log(array);

// Array com tipos diferentes de dados, mas não é recomendado. 
console.log([1,2,3,"teste", true]);

// length retorna o número de elementos de um array
console.log(array.length);
